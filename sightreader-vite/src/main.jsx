import React from "react";
import ReactDOM from "react-dom/client";
import SightReader from "./SightReader";

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <SightReader />
    </React.StrictMode>
);
